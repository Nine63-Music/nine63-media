/* Production-only site config. Leave assetBase empty for local dev.
   Media is served through Vercel rewrites that proxy from GitHub raw,
   fixing CORS, ORB, and rate-limiting issues. */
window.ACLASS_SITE = { assetBase: "https://nine63-music.github.io/nine63-media" };
