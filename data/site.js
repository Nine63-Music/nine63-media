/* Production-only site config. Leave assetBase empty for local dev.
   Before deploying, set it to the media CDN root (see README → Deploying),
   e.g. https://cdn.jsdelivr.net/gh/youruser/nine63-media@latest */
window.ACLASS_SITE = { assetBase: "" };
